# Telegram Tournament Manager

Простое приложение для управления турнирами в Telegram Mini App с использованием VK Cloud S3 для хранения данных.

## 🎯 Возможности

### Для всех пользователей:
- ✅ Просмотр списка турниров
- ✅ Просмотр деталей каждого турнира
- ✅ Информация о датах и статусах
- ✅ Автоматическое определение статуса (ожидается/в процессе/завершен)

### Для администраторов:
- ✅ Добавление новых турниров
- ✅ Указание названия и дат
- ✅ Безопасное хранение токена в SecureStorage

## 📋 Структура данных

### Турнир (Tournament)
```typescript
{
  id: string;              // Уникальный ID
  name: string;            // Название турнира
  startDate: string;       // Дата начала (YYYY-MM-DD)
  endDate: string;         // Дата окончания (YYYY-MM-DD)
  status: TournamentStatus; // 'upcoming' | 'in_progress' | 'finished'
  description?: string;    // Описание (опционально)
  participants?: number;   // Количество участников (опционально)
  winner?: string;         // Победитель (опционально)
}
```

### Хранилище
Данные хранятся в S3 бакете в файле `tournaments.json`:
```json
[
  {
    "id": "1707123456789-abc123",
    "name": "Зимний чемпионат 2024",
    "startDate": "2024-03-01",
    "endDate": "2024-03-15",
    "status": "upcoming"
  }
]
```

## 🚀 Установка и запуск

### 1. Установка зависимостей
```bash
npm install
```

### 2. Настройка S3 бакета

#### VK Cloud:
1. Создайте бакет в VK Cloud Object Storage
2. Настройте публичный доступ для чтения:
   - В настройках бакета включите "Публичный доступ"
   - Установите ACL для файла `tournaments.json` как `public-read`

3. Создайте учетные данные S3:
   - Access Key ID
   - Secret Access Key
   - Endpoint (например: `https://hb.ru-msk.vkcs.cloud`)
   - Region (например: `eu-central-1`)

### 3. Запуск в разработке
```bash
npm run dev
```

### 4. Сборка для продакшена
```bash
npm run build
```

## 🔐 Аутентификация администратора

### Формат токена
```
accessKeyId:secretAccessKey:endpoint:region:bucket
```

### Пример
```
AKIAIOSFODNN7EXAMPLE:wJalrXUtnFEMI/K7MDENG/bPxRfiCY:https://hb.ru-msk.vkcs.cloud:eu-central-1:my-tournaments
```

### Процесс входа
1. Пользователь нажимает "Войти как администратор"
2. Вводит токен S3
3. Приложение проверяет доступ к бакету
4. При успехе токен сохраняется в SecureStorage
5. Появляется кнопка "Добавить турнир"

## 📱 Интеграция с Telegram

### Настройка бота
1. Создайте бота через [@BotFather](https://t.me/botfather)
2. Настройте Web App:
   ```
   /setmenubutton
   → Выберите бота
   → Web App
   → Введите URL вашего приложения
   ```

### Используемые API
- **SecureStorage** - безопасное хранение токена администратора
- **HapticFeedback** - тактильные отклики при взаимодействии
- **WebApp.ready()** - инициализация приложения
- **WebApp.expand()** - разворачивание на весь экран

## 🛠 Tech Stack

- **React 18** + **TypeScript**
- **Vite** - сборщик
- **React Router** - навигация
- **Redux Toolkit** - управление состоянием
- **TanStack Query** - кэширование и синхронизация данных
- **Tailwind CSS** - стилизация
- **Radix UI** - UI компоненты
- **AWS SDK** - работа с S3
- **Telegram WebApp API** - интеграция с Telegram

## 📁 Структура проекта

```
src/
├── components/
│   ├── ui/                      # Базовые UI компоненты
│   │   ├── Button.tsx
│   │   ├── Input.tsx
│   │   └── Dialog.tsx
│   └── AddTournamentDialog.tsx  # Диалог добавления турнира
├── pages/
│   ├── TournamentsListPage.tsx  # Главная страница
│   ├── TournamentDetailPage.tsx # Детали турнира
│   └── AdminLoginPage.tsx       # Вход админа
├── hooks/
│   ├── useTelegram.ts           # Telegram WebApp API
│   └── useTournaments.ts        # React Query хуки
├── services/
│   └── s3Service.ts             # Сервис для работы с S3
├── store/
│   ├── authSlice.ts             # Redux состояние аутентификации
│   └── index.ts                 # Настройка store
├── types/
│   └── index.ts                 # TypeScript типы
└── utils/
    └── helpers.ts               # Вспомогательные функции
```

## 🎨 UI/UX особенности

- **Градиентные фоны** - красивый визуал
- **Smooth transitions** - плавные переходы
- **Responsive design** - адаптивная верстка
- **Status badges** - цветные бейджи статусов
- **Haptic feedback** - тактильные отклики
- **Loading states** - индикация загрузки

## 🔒 Безопасность

1. **SecureStorage** - токены хранятся в зашифрованном виде
2. **Валидация токена** - проверка перед сохранением
3. **Public read access** - данные турниров доступны всем для чтения
4. **Admin write access** - только с валидным токеном можно изменять данные

## 📝 Примеры использования

### Добавление турнира (администратор)
1. Войдите как администратор
2. Нажмите "Добавить турнир"
3. Заполните:
   - Название: "Весенний чемпионат"
   - Дата начала: 2024-04-01
   - Дата окончания: 2024-04-15
4. Нажмите "Добавить"

### Просмотр деталей
1. На главной странице нажмите на любой турнир
2. Откроется страница с подробной информацией
3. Нажмите "Назад" для возврата

## 🐛 Устранение проблем

### Турниры не загружаются
- Проверьте CORS настройки бакета
- Убедитесь, что файл `tournaments.json` имеет публичный доступ
- Проверьте консоль браузера на ошибки

### Не работает вход администратора
- Проверьте формат токена
- Убедитесь, что credentials правильные
- Проверьте права доступа к бакету

### Telegram WebApp не загружается
- Убедитесь, что приложение развернуто на HTTPS
- Проверьте, что скрипт Telegram загружен в index.html
- Откройте через Telegram, не через браузер напрямую

## 📦 Развертывание

### GitHub Pages (рекомендуется)

Самый простой способ развернуть приложение - использовать GitHub Pages с автоматическим деплоем:

1. Загрузите проект на GitHub
2. Настройте GitHub Pages в Settings → Pages
3. Выберите "GitHub Actions" как source
4. При push в main ветку приложение автоматически задеплоится

**Подробная инструкция**: см. [GITHUB_PAGES_DEPLOY.md](GITHUB_PAGES_DEPLOY.md)

### Vercel
```bash
npm i -g vercel
vercel --prod
```

### Netlify
```bash
npm i -g netlify-cli
netlify deploy --prod --dir=dist
```

## 🎯 Roadmap

- [ ] Редактирование турниров
- [ ] Удаление турниров
- [ ] Добавление участников
- [ ] Сетка турнира
- [ ] Результаты матчей
- [ ] Статистика
- [ ] Уведомления
- [ ] Экспорт данных

## 📄 Лицензия

MIT

## 🤝 Поддержка

При возникновении проблем создайте Issue в репозитории.
